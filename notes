
margin top and bottom should be percentages instead of rem/px

swap color selectors to checkboxes instead of divs

size could instead be a checkbox with a span inside with size

have a way to see what is selected

add to cart button

slider to select amount of t shirts

inside form -> checkbox
